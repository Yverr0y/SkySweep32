# TODO

All original core requirements and architectural changes have been met.
The project is currently stable at `v0.6.1`.

## Future Considerations
- [ ] Improve the TinyML model with project-specific `SignalDatabase` datasets (requires community data).
- [x] Complete LoRa Mesh multi-node triangulation math (ToD/RSSI) for operator localization (Done in v0.5.0: added trilateration solver inside MeshtasticClient)
